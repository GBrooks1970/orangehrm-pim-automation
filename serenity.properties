# Name the Serenity report after the project so the heading distinguishes it from
# a generic "Serenity BDD Report" (the default when this is unset). Read by the
# `serenity-bdd run` CLI from the repository root during `npm run test:report`.
serenity.project.name=OrangeHRM PIM Automation
